# Memory: 2026-03-29

## Finance Tracker Project (Iterative SDLC)
- **Status:** v5 (Savings Goals) is officially live in Production (`main`). v6 (CSV Uploads) is in PR.
- **Resolved:** Fixed the "Can't reach database server" error on Vercel by switching to the **Supabase Connection Pooler** (port `6543`, `?pgbouncer=true`, and `sslmode=require`).
- **v6 (CSV Uploads):**
    - Installed `papaparse`.
    - Implemented `api/transactions/bulk` with Prisma `createMany`.
    - Added CSV import UI in `Dashboard.tsx`.
    - Opened **PR #18** for v6 Foundation.
- **Automation:** 
    - Updated PR monitor Cron job with `delivery.channel="telegram"` to send real-time notifications to `830692749`.
    - PR monitor is now watching **PR #18**.
- **Next Move:** Wait for CodeRabbit review/CI and merge v6.

## Configuration & Setup
- **Telegram Integration:** User ID `830692749`. Fixed pairing/delivery issues.
- **Vibe:** Maintaining the `afplay /System/Library/Sounds/Ping.aiff &` rule for response feedback.
- **Tone:** Keep it chill and direct (BroBot persona).
